[build]
  command = "npm run build:full"
  publish = "dist"
  functions = "functions"

[dev]
  command = "npm run dev"
  functions = "functions"
  publish = "dist"

  conditions = {Path = {exclude = ["/.netlify/functions/*"]}}